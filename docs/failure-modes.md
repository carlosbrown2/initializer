# Failure-mode register

Every module, function, and data flow that can fail must appear here, paired with a mechanical check that catches the failure before merge. See `project-kickoff-prompt.md` §1 for the contract.

**Status values:** `covered` | `proven-impossible` | `out-of-scope`

**Categories:** `correctness` | `concurrency` | `atomicity` | `input` | `resource` | `temporal` | `version` | `dependency` | `operational` | `security`

| Module / function | Failure mode | Category | Check (file:test) | Status |
|-------------------|--------------|----------|-------------------|--------|

<!-- Below: this template's own dogfooding rows. Downstream projects bootstrapped from this template should review and prune — the table above ships empty for downstream and is filled in as project failure modes are caught. -->

| scripts/ralph/ralph.sh gate-result parsing | agent emits `<gate-result>PASS</gate-result>` without running the gate (Goodhart / gate-bypass). The self-report was a proxy for the property "gate passed": a prediction the agent made about the gate, not a measurement of it. Binding `.last-gate-result` to the parsed tag made the file a proxy for "what the agent said" rather than "what the gate actually returned" | correctness | scripts/ralph/ralph.sh now runs the gate itself via scripts/ralph/lib.sh run_gate (sourcing scripts/hooks/parsers.sh gate_command_extract) on every BEAD_DONE iteration and writes the real exit code (PASS/FAIL/SKIPPED) to .last-gate-result; the `<gate-result>` emission requirement is deleted from scripts/ralph/prompt.md so the agent can no longer spoof the result. scripts/hooks/install.sh pre-push hook remains as defense-in-depth: it re-runs the same gate on push and still blocks on divergence against .last-gate-result (now catching env drift between iteration and push rather than agent dishonesty). tests/hooks/ralph.bats exercises run_gate for PASS / FAIL / SKIPPED / overwrite-not-append / SKIPPED-distinct-from-FAIL | covered |
| scripts/hooks/parsers.sh register-integrity parsers | edge cases (escaped pipes, Unicode, trailing whitespace, 5-column rows, multi-line continuation rows) silently accept malformed rows or reject valid ones; drift between the inlined pre-commit awk and the callable parsers | correctness | tests/hooks/parsers.bats exercises every parser against known-good and known-bad fixtures plus smoke tests against the real registers; scripts/hooks/install.sh sources parsers.sh so there is only one implementation | covered |
| docs/skills/review-rubric.md (Phase 1 completion) | unedited starter rubric ships with the project, letting "Review verdict" be claimed `bounded` while severity classification is still anchored only to generic clauses (Goodhart). A naive guard that only matches the disclaimer phrase admits stub rubrics with no clauses and rubrics that copy the starter clauses verbatim | correctness | scripts/hooks/parsers.sh rubric_edit_check rejects rubrics that (a) still contain the starter disclaimer, (b) keep the starter H1 header verbatim, (c) define no clauses at all, or (d) define only clauses already in the starter allowlist; scripts/hooks/install.sh wires it into the pre-commit hook once any bead is in_progress; tests/hooks/parsers.bats exercises all four reject paths plus the accept path; Phase 1 bootstrap (no in-progress bead) is exempt | covered |
| CLAUDE.md verification gate command | soft-fail escape in a correctness chain — trailing `\|\| <always-succeed>` (`\|\| true`, `\|\| :`, `\|\| 0`, `\|\| exit 0`, etc.) binds to the whole `&&` chain so any in-chain failure silently exits 0; or a non-bash file (markdown) is added to a `bash -n` chain forcing a soft-fail escape. The pre-push hook re-runs whatever the gate is, so a no-op gate makes the hook a no-op too. The bug class name is broader than the original `\|\| true` label because `\|\|` precedence makes every `<always-succeed>` trailer structurally identical | correctness | tests/hooks/gate.bats asserts the extracted gate command has no soft-fail-escape tail (via scripts/hooks/parsers.sh gate_has_soft_fail_escape which covers `\|\| true`, `\|\| :`, `\|\| 0`, `\|\| exit 0`), contains no `.md` paths, passes against the current repo, and propagates non-zero exit when any in-chain clause is corrupted | covered |
| tests/hooks/gate.bats "gate passes against the current repo" sanity test | the gate now invokes `bats tests/hooks/`, which re-invokes this very test file. Without a recursion guard the sanity test would fork-bomb the gate invocation into infinite nested bats runs | correctness | tests/hooks/gate.bats sentinel `_BATS_GATE_REENTRY`: outer invocation exports the flag before running the gate; the nested bats run sees the flag and skips the sanity test so only the outer run exercises the full chain against the real repo | covered |
| scripts/hooks/parsers.sh syntax errors | a syntax error in parsers.sh silently breaks every register-integrity check (install.sh sources it at runtime); without a gate clause, the failure surfaces only when a developer hits pre-commit on an affected register edit | correctness | CLAUDE.md verification gate includes `bash -n scripts/hooks/parsers.sh`; tests/hooks/gate.bats "gate fails when scripts/hooks/parsers.sh has a syntax error" corrupts parsers.sh in a temp tree and asserts the chain exits non-zero, which also fail-closes a future accidental drop of the parsers.sh clause from the gate | covered |
| scripts/hooks/install.sh review artifact validator | validator only matched clause-shape (`P[123]\.[a-z][a-z-]*`) and did not check membership in the rubric, so a review artifact citing `P1.totally-made-up-clause` passed exit 0 — turning the "Review verdict" decision-register row into a Goodhart on clause-shape rather than on the actual canonical clause set | correctness | scripts/hooks/parsers.sh review_artifact_clauses_check extracts the canonical clause set from `**P[123].name**` bold-marker definitions in docs/skills/review-rubric.md and rejects any cited clause not in that set; scripts/hooks/install.sh wires it into the review-artifact validator so review-bead commits with invented clauses block; tests/hooks/parsers.bats covers reject (invented clause), accept (real clauses), accept-after-rubric-add (no separate registration), reject-when-rubric-empty, plain-prose-citation reject, and a smoke test that synthesizes an artifact citing every real clause from the shipped rubric | covered |
| CLAUDE.md "## Verification Gate" extractor duplication | the awk that extracts the gate command was duplicated in scripts/hooks/install.sh (pre-push hook heredoc) and tests/hooks/gate.bats (setup). Drift between the two makes the gate.bats "gate passes against the current repo" sanity test a proxy for "what gate.bats extracts," not "what the pre-push hook extracts" — so a gate.bats extractor that silently extracts nothing would make every gate assertion vacuously pass while the real pre-push hook happily re-runs a drifted gate | correctness | scripts/hooks/parsers.sh gate_command_extract is the single implementation; the pre-push heredoc in scripts/hooks/install.sh sources parsers.sh and calls the function; tests/hooks/gate.bats sources parsers.sh in setup; tests/hooks/gate.bats also asserts (a) install.sh's pre-push heredoc uses gate_command_extract, (b) neither install.sh nor gate.bats re-inlines the awk anchor pattern, (c) end-to-end equivalence: the heredoc-extracted pre-push body's GATE_CMD matches gate_command_extract's output byte-for-byte against the same CLAUDE.md | covered |
| scripts/ralph/ralph.sh routing logic | confidence parsing, auto-land policy matrix, retry-tracking transitions, and REWORK prerequisite extraction had no mechanical coverage — only `bash -n scripts/ralph/ralph.sh`. Two latent bugs survived: `read_auto_land_policy` used `grep -A1 "## Confidence Routing"` which misses the auto-land line across a blank line (silently returns the default regardless of the real setting), and the REWORK prereq regex with `head -1` returned the active bead id from the "X depends on:" header line instead of the real prerequisite (re-open step was a no-op, review bead looped) | correctness | scripts/ralph/lib.sh extracts compute_confidence / read_auto_land_policy / should_auto_land / compute_retry_state / extract_prereq_bead_id as a sourceable library; scripts/ralph/ralph.sh sources it at startup (fails closed if absent); tests/hooks/ralph.bats exercises each function with the bead-flagged adversarial cases (blank line between heading and value, commented-out alternative, unknown policy fallback, MAX_RETRIES boundary, active-bead exclusion in dep-list output) plus compute_confidence's gate-FAIL/SKIPPED/empty terminal-LOW rule, each downgrade axis in isolation, the diff=500 boundary, two-axis LOW, and the three-axis LOW floor; the bats suite already runs under `bats tests/hooks/` in the CLAUDE.md verification gate | covered |
| scripts/ralph/ralph.sh BEAD_DONE confidence.log bead= field | confidence.log recorded `bead=unknown` on successful BEAD_DONE iters via two distinct root causes that produced the same downstream symptom: (a) `_RALPH_ACTIVE_BEAD` was captured twice per iteration and the second read came back empty after the agent had `bd close`d the bead (snapshot-timing bug — signature in confidence.log was iter=1..12 all `bead_done=true bead=unknown`, interspersed with stalled iters showing real ids); (b) the BEAD_DONE log line referenced `${_RALPH_ACTIVE_BEAD:-unknown}`, which is empty by design whenever the iter started with no in-progress bead and the agent picked up a fresh bead via `_ralph_bead_ready` during the iter (variable-choice bug — the snapshot-timing fix corrected the *when* of the read but kept the wrong *which* in the log line). archive_schema_check filters `bead=unknown` entries out before requiring archive blocks, so both bugs were silent — the parser passed on the wrong input. Together they made confidence.log useless for per-bead auditing | correctness | scripts/ralph/ralph.sh captures `_RALPH_ACTIVE_BEAD` exactly once at the top of each iteration AND binds the BEAD_DONE confidence.log lines to `${_RALPH_BEAD_ID:-unknown}` (set from the resumed active bead OR `_ralph_bead_ready`, so it always names the bead the agent will work on). The snapshot-volatile-state-once pattern is promoted to CLAUDE.md Discovered Patterns. tests/hooks/ralph.bats covers both BEAD_DONE log lines (auto-land branch and confidence=NONE branch): extracts the real source line via awk and eval's it under the bug condition (`_RALPH_ACTIVE_BEAD=""`, fresh `_RALPH_BEAD_ID`), asserting `bead=<picked-up-id>` not `bead=unknown` | covered |
| scripts/hooks/install.sh bead-type gate, bd extraction | the pre-commit hook conditioned on `IN_PROGRESS_BEAD` which was populated by `bd list | grep -o ... | head -1 || true`. If bd's human-readable format drifted, the grep matched nothing and the entire gate chain (bead-type, rubric-edit, scope, review artifact) silently no-opped. A malformed bd output was indistinguishable from "no bead in progress," which is exactly the state that disables the chain | correctness | scripts/hooks/parsers.sh bd_bead_in_progress uses `bd list --status=in_progress --json \| jq` and returns non-zero on extraction failure (bd errored or produced non-parseable JSON). scripts/hooks/install.sh pre-commit sources the function and BLOCKS the commit on non-zero — fail-closed rather than fail-open. Phase 1 bootstrap (no bd installed) still passes because the function short-circuits on `command -v bd`. tests/hooks/parsers.bats covers the non-parseable-JSON reject path and the no-bd-installed accept path | covered |
| scripts/hooks/install.sh commit-msg format | the commit-msg hook only validated the `<type>:` prefix. A commit of `feat: banana` passed even though prompt.md advertises the `<type>: [bead-id] - <title>` format for bead commits. The hook was theater — the format was a prompt-level suggestion with no structural backstop, so "commit message format" was `ritual-bounded` dressed as `bounded` | correctness | scripts/hooks/install.sh commit-msg hook now enforces the bracketed `[bead-id] - <title>` shape when the message starts with `[`, using a regex sourced from BEAD_ID_REGEX. Non-bead commits (no `[` after the type prefix) still pass if the description is non-empty. tests/hooks/parsers.bats covers the accept / reject matrix for both forms | covered |
| scripts/ralph/archive.txt machine-readability | prompt.md mandates the agent append a `## <date> - <bead-id>` block to archive.txt after each BEAD_DONE iteration, but nothing downstream parsed it. "Agent updated the log" was a proxy for "the log is useful"; the agent could write anything (or nothing structured) and the gate stayed green. Future agents discovering prior decisions had to trust that each block was present, complete, and findable | correctness | scripts/hooks/parsers.sh archive_schema_check asserts that every `bead_done=true` entry in confidence.log with a real bead id (not `unknown`) has a matching `## YYYY-MM-DD [HH:MM] - <bead-id>` block in archive.txt. Wired into the verification gate so a BEAD_DONE without a corresponding archive block fails the next push. Phase 1 bootstrap (no confidence.log) and pre-fix logs (all `bead=unknown`) pass. Smoke tested in tests/hooks/parsers.bats | covered |
| scripts/ralph/lib.sh vs scripts/hooks/parsers.sh bead id regex | bead id regex was inlined in two libraries with drifted values (lib.sh used `[a-z][-a-z0-9]*-[a-z0-9]{2,}`, install.sh grep used `[a-z_]*-[a-z0-9]*-[a-z0-9]*`). Each was individually plausible; together they disagreed about what counts as a bead id. install.sh could see a bead the REWORK prereq extractor couldn't, or vice versa. The drift was silent because no test compared the two | correctness | scripts/ralph/lib.sh `BEAD_ID_REGEX` and scripts/hooks/parsers.sh `PARSERS_BEAD_ID_REGEX` are the single-source constants each library reads. A smoke test in tests/hooks/parsers.bats asserts the two values are equal byte-for-byte; a CLAUDE.md invariant documents the rule | covered |
| external bd CLI version drift | every load-bearing hook and loop path parses `bd` output. bd is an external dep with no pinned version — a minor bump that changes the --json schema or renames a field silently breaks extraction. bd_bead_in_progress returns empty; the bead-type gate no-ops; every downstream check silently passes. The previous install.sh grep-based extractor was the symptom, not the cause: the root is "load-bearing parsing of an unpinned external contract" | version | CLAUDE.md § Pinned Tool Versions declares `bd >= 0.3.0`; the verification gate asserts the pin via `bd --version \| grep -qE '^bd version ([1-9]\|0\.([3-9]\|[1-9][0-9]))'`. A bd downgrade or a bd format change predating 0.3 surfaces as a gate failure on every pre-push and on every CI run | covered |
| scripts/hooks/install.sh review file iteration | review-artifact validator iterated via `for f in $REVIEW_FILES; do`, which unquoted-expanded and word-split on IFS. A review artifact at `docs/reviews/story-abc my notes.md` would have been split into two tokens, corrupting the loop and potentially bypassing the citation / clause-membership check. The asymmetry with the scope-enforcement loop (which used quote-safe `while IFS= read -r`) was a code smell waiting to be the bug report | correctness | scripts/hooks/install.sh pre-commit review-artifact block reads file list from `git diff --cached --name-only --diff-filter=AM -z` and iterates with `while IFS= read -r -d ''`. Every filename — including those with spaces, tabs, or newlines — is passed whole to the downstream checks | covered |
| scripts/ralph/ralph.sh per-iter HEAD reads | agent emits `<promise>BEAD_DONE</promise>` without committing — every per-iter signal (diff_lines / touched_hooks / touched_claude_md) reads HEAD without verifying HEAD has moved since iter start, so the iter silently grades the *prior* commit's diff and credits the result to the wrong bead. compute_confidence cannot detect cross-bead contamination on its own. Signature pattern in confidence.log: a 28-min iter gap with `bead_done=true` whose touched_* signals match the previous bead's commit instead of the current one | correctness | scripts/ralph/lib.sh compute_head_unchanged_for_bead_done compares pre-iter HEAD (captured before the agent runs) against post-iter HEAD (captured at BEAD_DONE detection); scripts/ralph/ralph.sh sets `_RALPH_STALE_HEAD=true` on unchanged, forces `_RALPH_GATE_RESULT=FAIL`, writes `FAIL` to `.last-gate-result` (so the pre-push hook also rejects the push), and emits `stale_head=true` into both confidence.log echo branches (auto-land and confidence=NONE). tests/hooks/ralph.bats covers the helper (pre==post → unchanged, pre!=post → moved, real temp git repo for both, both-empty edge case) plus integration tests that extract ralph.sh's gate-result block and eval it under stale-HEAD state, asserting both `_RALPH_GATE_RESULT=FAIL` and `.last-gate-result=FAIL` (with a stub run_gate so a regression that lets the gate run anyway is caught), the complementary non-stale path stays PASS, and confidence.log carries `stale_head=true` on stale iters / omits the field on healthy iters | covered |
| scripts/ralph/ralph.sh touched_claude_md axis | the prior signal proxied "did CLAUDE.md change" via `grep -qx CLAUDE.md` against the HEAD file list, which fired on every compound bead that promoted a `model:`-tagged entry to `## Discovered Patterns`. That section is by-design append-only output and redefines no rule the gate runs, so the axis was downgrading exactly the iters whose bind on the gate's truth is unchanged — saturating the quartet pattern at MEDIUM regardless of work quality. Edits to `## Invariants` or `## Verification Gate`, by contrast, can shift the gate's own definition in the same diff being evaluated, and that risk is what the axis is designed to catch. The bug class is "axis bound to a proxy (file-name match), not to the property (rule-shifting edit)" | correctness | scripts/ralph/lib.sh claude_md_touched_outside_patterns reads HEAD~1:CLAUDE.md and HEAD:CLAUDE.md, strips `## Discovered Patterns` through the next top-level `## ` (or EOF) on each side via awk (`/^## Discovered Patterns[[:space:]]*$/{f=1;next} /^## /{f=0} !f`), and compares the residue. File-added or file-deleted (XOR via `git cat-file -e`) short-circuits to true so the rule-file appearing or disappearing still fires the axis. scripts/ralph/ralph.sh calls the helper at the touched-claude-md detection point in place of the prior `grep -qx CLAUDE.md`. tests/hooks/ralph.bats covers nine cases against real temp git repos: pattern-only append (false), pattern-only deletion (false), `## Invariants` body edit (true), `## Verification Gate` clause edit (true), no-CLAUDE-change (false), file-added (true), file-deleted (true), prose-and-pattern straddling edit (true), and a live-repo smoke that asserts boolean shape only | covered |
| scripts/hooks/parsers.sh register-cited symbol resolution | a register row whose Check / Enforcement / Bounding-mechanism cell cites `path/to/file::symbol` silently breaks its bind when a pare-down deletes or renames `symbol` while the file still exists. fm_file_refs_check / dec_file_refs_check stop at file existence and treat the `::symbol` suffix as opaque, so the row's coverage claim becomes a dangling reference but pre-commit passes. The CLAUDE.md discipline "Register Check-cell citations bind the named symbol's existence-by-name" was held by manual cross-referencing — the discipline scales until it doesn't, exactly like every prior `ritual-bounded → bounded` promotion. Substring-impostor variants (`test_boom` cited but only `test_boom_extended` exists) are the same class with a sneakier presentation | correctness | scripts/hooks/parsers.sh register_symbol_refs_check walks `<path>::<symbol>` tokens in both registers and asserts the symbol is defined in the cited file as one of: `def <symbol>(`, `async def <symbol>(`, `class <symbol>(` / `class <symbol>:`, module-level `<symbol> = …`, or annotated module-level `<symbol>: <type>`. Every accepted form requires a non-symbol delimiter immediately after `<symbol>` ((, :, =, or whitespace+:), so `test_boom` cannot match against `test_boom_extended`. Skips refs whose file is missing on disk (delegated to fm_file_refs_check / dec_file_refs_check — composability rather than overlap) and refs whose file is gitignored (no checked-in source). Lists every dangling ref so a single commit can fix them in batch. scripts/hooks/install.sh wires the check into the pre-commit chain immediately after each register's file-refs-check. tests/hooks/parsers.bats covers accept (def / async def / class with bases / class without bases / module-level assignment / annotated module-level), reject (deleted symbol, renamed helper, substring impostor for both def and assignment forms, every-offender-listed), and skip (missing file, gitignored file) cases plus .bats / .md file types and smoke tests against the live registers | covered |
| scripts/ralph/ralph.sh stale governance bead routing | governance beads (titles matching `^(Observe\|Audit\|Decide\|Triage\|Review the loop)`) sit in `bd ready` indefinitely because they get the same priority as module work; the loop preferentially picks the next module bead and ignores its own audit instructions for days. The 2026-04-23 `Observe ralph loop for 20 impl beads; decide on research-phase split` bead was filed precisely to ask the question that surfaced the runaway-loop pattern and the loop never picked it up — six days of audit drift before the operator noticed. Failure shape: operator-visibility loss (no signal that an audit instruction is going stale before the operator next looks at the loop). The earlier >7d force-LOW predicate was demoted in agent-template-79f as belt-and-suspenders — surfacing alone changes the operator's next action | operational | scripts/ralph/ralph.sh `_ralph_surface_stale_governance` snapshots `bd list --status=open --json` once per iter and prints every matching bead older than 3 days under `⚠ Stale governance:` above the iter banner. tests/hooks/ralph.bats covers the surfacing helper accept (>3d matching bead prints under header), silence (≤3d matching, >3d non-matching, empty input from bd-call failure), and the title regex accept set indirectly via the matching-bead test. The `_ralph_surface_stale_governance` source line in scripts/ralph/ralph.sh is the only caller of `GOVERNANCE_TITLE_REGEX` and `BD_DATE_TO_EPOCH_JQ_DEF` after the demotion | covered |
| scripts/ralph/ralph.sh bead-context fields in confidence.log | new per-iter visibility surface (`bead_type=`, `title="…"`, `completed="…"`) opens three structural failure modes covered by one bind cluster: (a) `_ralph_load_bead_meta` / `_ralph_bead_title` jq path `.title // empty` silently empties on bd >=0.49 because the CLI returns an array-of-one rather than an object, so the banner and the log line carry blank titles even though bd is healthy — this was the original `_ralph_bead_title` bug masked by the legacy line-2 sed fallback; (b) free-form titles, descriptions, or commit subjects containing newlines / tabs / embedded `"` corrupt the line-oriented parsers in scripts/hooks/parsers.sh (e.g. archive_schema_check's `grep -oE 'bead=[^ ]+'`) by smuggling a newline mid-line or by closing the `title="…"` quote early — every downstream field reads garbage; (c) the new `bead_type=<word>` field could collide with a parser regex that loosely matches `bead[^ ]+` instead of `bead=[^ ]+`, silently extracting the wrong token from the log line | correctness | scripts/ralph/ralph.sh `_ralph_load_bead_meta` and `_ralph_bead_title` use `(if type == "array" then .[0] else . end).<field>` jq paths so bd <0.49 (object) and bd >=0.49 (array-of-one) shapes both populate the globals; `_ralph_sanitize_log_field` runs free-form text through `tr '[:space:]' ' ' \| tr '"' "'" \| tr -s ' '` and truncates at 160 chars with `...` before insertion into `title=` and `completed=` fields, so a smuggled newline / tab / `"` cannot corrupt the line; tests/hooks/ralph.bats covers both bd shapes via a stub bd on PATH, sanitizer edge cases (whitespace collapse, `"` → `'`, 160-char boundary at-and-above, empty input), all five log echo branches (BLOCKED / REWORK / ESCALATION / BEAD_DONE-with-confidence / BEAD_DONE-without-confidence) carry `bead_type` + sanitized `title`, both BEAD_DONE branches additionally carry sanitized `completed`, banner shape (`[type] — title` plus optional `Description:` line, gated on description non-empty), iteration footer prints bead context only when `_RALPH_COMPLETED_SUMMARY` is non-empty, archive_schema_check still extracts the correct bead id from a confidence.log line that also has `bead_type=<word>`, and `_ralph_cleanup` unsets the three new globals so sourcing does not leak state | covered |
| scripts/ralph/ralph.sh _ralph_load_bead_meta zsh-source compat | sourcing scripts/ralph/ralph.sh from a zsh shell under `set -u` crashed at `_RALPH_BEAD_TYPE="${BASH_REMATCH[2]}"` with `BASH_REMATCH[2]: parameter not set` because zsh does not populate BASH_REMATCH by default, and even with `setopt BASH_REMATCH` its array indices are shifted by 1 vs bash so the second capture group is unset. The crash blocked every operator-side `source scripts/ralph/ralph.sh` from a zsh-default macOS shell (tab-completion helpers, ad-hoc debug sourcing, harness pre-flight) before the loop could even start. Bug class: bash-only construct used in a script the operator is expected to source from any login shell | dependency | scripts/ralph/ralph.sh `_ralph_load_bead_meta` replaced the regex-with-capture-group extraction with a loop over the loop-taxonomy keywords (`pare-down impl review pare compound research`) that uses `[[ "$_RALPH_BEAD_TITLE" =~ (^\|[[:space:]])${_kw}[[:space:]]*: ]]` only as a boolean test — no `BASH_REMATCH` access, so works identically under bash and zsh. The keyword loop puts `pare-down` before `pare` so the longer prefix wins (a swap to alphabetical order would silently mis-resolve `pare-down: …` titles to `pare`). tests/hooks/ralph.bats "every loop-taxonomy keyword resolves from the title" exercises every keyword end-to-end so any regression that re-introduces capture-group access, drops a keyword, or reorders the loop to put `pare` first is caught at test time (under bash); the zsh-source crash itself is mechanically prevented by the absence of `BASH_REMATCH` in the new source line | covered |

<!-- Rows are added as the template grows. Each bead's DoD includes adding a row here once the check ships. -->
